{
  "name": "grenier-en-ligne",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "sass --no-source-map style/main.scss public/style/main.css",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "docker-compose kill && APP_ENV=dev docker-compose up --build",
    "start:prod": "docker-compose kill && APP_ENV=prod docker-compose up --build",
    "start:preprod": "docker-compose kill && APP_ENV=preprod docker-compose up --build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-gyp": "^9.4.0",
    "sass": "^1.63.4"
  }
}
